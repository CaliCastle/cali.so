# Implementation plans

Generated by the `improve` and `improve-codebase-architecture` advisor
workflows on 2026-07-18, reconciled against commit `59a39bc`. The existing
`plans/` directory is reserved for animation work, so general and architecture
plans live here.

Execute active plans in the order below unless the dependency column says they
may run in parallel. Each executor must read its plan fully, honor its STOP
conditions, run every verification gate, and update only its status row here
when done. A `REJECTED` row is an audit record, not scheduled work; refresh its
plan and return it to an active status before execution.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| [001](001-stabilize-unit-test-baseline.md) | Consolidate unit-test infrastructure | P1 | M | none | TODO |
| [002](002-retire-sanity-port-tooling.md) | Retire the completed Sanity porting tool | P1 | S | none | TODO |
| [003](003-remove-unreachable-v3-residue.md) | Remove unreachable v3 modules and artifacts | P1 | S | none | TODO |
| [004](004-remove-design-switchboards.md) | Remove the dormant icon and shape switchboards | P1 | M | 003 | TODO |
| [005](005-deepen-site-profile.md) | Put fork identity behind one Site Profile interface | P1 | L | none | TODO |
| [006](006-gate-optional-operator-stack.md) | Make the Operator Stack optional per site | P1 | L | 005 | TODO |
| [007](007-document-and-verify-forks.md) | Document and verify the supported fork workflow | P1 | M | 002, 004, 005, 006 | TODO |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED: <reason>`, or
`REJECTED: <reason>`.

## Dependency notes

- 001 rejects a global worker cap because the unchanged `59a39bc` suite passed
  three consecutive runs at 107 files and 1,007 tests. Its independent shared
  PGlite-fixture and Quality-CI deduplication work remains active.
- 001, 002, and 003 may run in parallel immediately.
- 004 follows 003 because three unreachable UI modules still import the
  dormant icon and shape machinery. Deleting them first shrinks the live
  migration surface.
- 005 may begin immediately, but executing it after the deletion plans keeps
  its identity inventory and diff smaller.
- 006 depends on the committed Site Profile interface created by 005.
- 007 runs last so its setup guide, replacement inventory, and checker describe
  the actual post-cleanup repository.
- Animation plans 005 through 008 are indexed separately in `plans/README.md`.

## Confirmed architecture decisions

- The supported template is public-first with an optional full Operator Stack.
- All non-secret fork identity lives in one committed, typed
  `site.config.ts`. Secrets and environment-specific provider settings remain
  in environment variables.
- Personal posts, photographs, projects, and branding stay in the main
  repository as the working example. Fork documentation marks their
  replacement as mandatory; there is no second template branch.
- `operatorStack: false` makes operator and operator-published routes return
  404 and prevents provider/environment initialization. It does not physically
  delete their source or packages from a clone that can also set the profile to
  `true`.
- The old bilingual v2 cloning article is rewritten as a concise v3 overview
  pointing to one canonical `docs/forking.md`.
- Fork setup has a read-only checker. There is no mutating initializer or
  generator.
- The target v3 architecture has no icon-library abstraction. Existing
  hand-picked direct imports stay as chosen. The dormant icon and radius
  switchers are removed without a visible design change.

## Findings considered and rejected

- Delete `content/legacy-url-manifest.json`, redirects, rewrites, retired
  confirmation routes, archived newsletters, or historical post assets:
  rejected because they are active URL/publication contracts, not dead source.
- Rewrite or delete applied database migrations and snapshots: rejected because
  they are immutable deployment history.
- Drop legacy production tables in this bundle: deferred to a separately
  approved contract-migration effort with archival proof and the required
  production confirmations.
- Ship a separate template branch: rejected because it would drift from the
  personal site that is meant to remain the working example.
- Add a destructive fork initializer: rejected because the safe supported path
  is explicit configuration, documentation, and read-only verification.
- Support arbitrary locale sets in this cleanup: rejected as scope expansion.
  The current paired Chinese/English route contract remains the supported v3
  template contract.
- Physically tree-shake the Operator Stack packages from public-only clones:
  rejected without a separate distribution/build system. The selected profile
  removes reachability and infrastructure requirements, not source ownership.
